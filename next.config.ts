import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ['scorpionic-jeannine-ideologically.ngrok-free.dev'],
};

export default nextConfig;
